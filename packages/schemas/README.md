# @viralcraft/schemas

Shared TypeScript types for the ViralCraft pipeline.

These are placeholder definitions. When fields evolve, update the matching
Pydantic models in `apps/api`, plus any sample JSON in `data/`, per the
project's schema rules.

Exports:

- `VideoAnalysis`, `SceneSegment`
- `StructureCard`, `EditingAtom`
- `Storyboard`, `StoryboardScene`
- `RenderJob`
